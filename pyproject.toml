[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "semantic-retrieval-engine"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["infrastructure", "retrieval", "pipelines"]

[tool.pytest.ini_options]
pythonpath = ["src"]
